# ======================
# Keybindings
# ~/.config/zsh/06-keybindings.zsh
# ======================

# Disable Atuin auto-binding (backup besides --disable-up-arrow)
export ATUIN_NOBIND=true

# Note: actual bindkey calls are in 40-prompt.zsh, after all evals,
# to guarantee nothing overrides them afterwards.